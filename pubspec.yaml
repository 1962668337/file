name: flutter_file_manager
description: A new Flutter application.

#name: flutter_video_compress_example
#description: Demonstrates how to use the flutter_video_compress plugin.
#publish_to: 'none'

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# Read more about versioning at semver.org.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.1.4 # 
  cupertino_icons: ^0.1.2 # 
  path_provider: ^0.4.1   # 
  #path_provider: ^2.0.2
  open_file: 2.1.1  # 
  video_player: 0.10.11+1 # 


  permission_handler: ^3.3.0  # 
  intl: ^0.16.0  # 
  fluttertoast: ^3.1.3   # 
  gzx_dropdown_menu : ^2.0.0  # 
  
  #photo_view: ^0.7.0 # 
  photo_view: ^0.11.1 # 

  multi_image_picker: ^4.8.00   # 
  image_picker: ^0.7.4   # 
  
  dio: ^2.1.10  #
  rxdart: ^0.22.0

  animations: ^1.1.1 #
  orientation: ^1.3.0 #
  
  titled_navigation_bar: ^4.1.0  # 
  bottom_bar_with_sheet: ^2.0.0  # 

  carousel_slider: ^1.3.1 #
  flutter_html: ^0.11.1 #
  html: ^0.14.0+3 #
  sqflite: ^1.1.0 #
  color_thief_flutter: ^1.0.2 #
  flutter_redux: ^0.5.3 #
  shared_preferences: ^0.5.4+8 #

  scroll_to_index: ^2.0.0 #
  transformer_page_view_tv: 0.1.7-nullsafety #
  simple_animations: 3.1.1 #
  matrix_gesture_detector : 0.2.0-nullsafety.1 #
  flutter_card_swipper : 0.4.0 #
  animated_text_kit : 4.2.1 #

  chewie: ^0.9.7  # 
  neeko: ^0.0.10 # 
 


dev_dependencies:


  build_runner: ^2.0.5 #
  build_web_compilers: ^3.0.0 #

  
  flutter_test:
    sdk: flutter

  flutter_video_compress: ^0.3.7+8 # 视频缩略图
 




# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
   - static/images/InsteadOfVideo.png 
   - static/images/InsteadOfVideoOne.png
   - static/images/InsteadOfVideoThree.png
   - static/images/InsteadOfVideoTwo.png

   - assets/images/



   - assets/videos/

   - static/videos/Butterfly-209.mp4
   - static/videos/1.mp4

      